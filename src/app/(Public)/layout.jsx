
export const metadata = {
    title: {
        template: '%s | Next Base',
        default: 'خانه',
    },
    description: 'صفحه اصلی Next Base',
};

export default function PublicLayout({ children }) {
    return children;
}